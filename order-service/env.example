# Database Configuration
ORDER_DB_HOST=order-postgres
ORDER_DB_PORT=5434
ORDER_DB_USER=postgres
ORDER_DB_PASSWORD=postgres
ORDER_DB_NAME=orders
ORDER_DB_SSL_MODE=disable

# Server Configuration
ORDER_SERVER_HOST=0.0.0.0
ORDER_SERVER_PORT=8080

# Debug Mode
ORDER_DEBUG=false
